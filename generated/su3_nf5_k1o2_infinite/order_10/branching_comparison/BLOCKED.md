# Recorded branching-comparison blocker

The order-10 comparison was intentionally stopped because the shared `_terms` parser retained only the first `irreducible_representations` entry and the generator then imposed a single length-five Dynkin-label check.  The authoritative UV PL has ordered `A4` and `A1` factors, of lengths four and one; flattening them is not permitted.
