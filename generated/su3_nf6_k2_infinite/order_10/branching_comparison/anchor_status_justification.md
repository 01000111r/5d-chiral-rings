# Anchor status justification

## Instanton current — retain

The exact adjoint restriction contains `[0,1,0,0,0]` once at `(x,q)=(2,0)`. Signed `k=-2` gives `B(E_-)=-3-k=-1`, `I(E_-)=1`. The conjugate `[0,0,0,1,0]` occurs at `(-2,0)`.

## Classical baryon — retain

The corrected restriction of `[0,0,0,0,0,1]` still contains the self-conjugate SU(6) 20 `[0,0,1,0,0]` once at `(x,q)=(0,1)`. The finite `beta^1` term fixes `(B,I)=(3,0)`. Its sign is not inferred from the self-conjugate Dynkin label.

Neither anchor changes; `change_justified` is false for both.
